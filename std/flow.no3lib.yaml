category: "Flow"
lib: "Std"
nodes:
  - title: "Main"
    desc: "Runs on program start."
    exec_kind: !OutCondition main
    fields:
    inputs:
    outputs:
    impls:
      - lang: "python3"
        code: ""

  - title: "Split"
    desc: "Runs both paths"
    exec_kind: !InChooseOut 2
    fields:
    inputs:
    outputs:
    impls:
      - lang: "python3"
        code: ""
